import { Meteor } from 'meteor/meteor';

//need to include db for the server
import { Nodes } from '../imports/api/db.js';
import { Paths } from '../imports/api/db.js';

Meteor.startup(() => {
  // code to run on server at startup
});
